# 大会シート管理データ v2

`taikai_manage` Issue #46〜#55と`docs/TOURNAMENT_SHEET_AUDIT.md`に基づく、
1大会1シートの管理データ構造である。

## 物理配置

フォーム回答はシート上部にそのまま残す。A列の連続タイムスタンプが終わった行を
回答終端とし、空行を1行空けた後、左端A列から管理ブロックを置く。

管理ブロックは`__TAIKAI_MANAGEMENT_V2__`で開始し、
`__TAIKAI_MANAGEMENT_V2_END__`で終了する。列位置はフォーム質問数に依存しない。

```text
1行目       Googleフォーム回答ヘッダーだけ
2〜R行目    Googleフォーム回答原本（A列タイムスタンプが連続）
R+1行目     空行
R+2行目〜   A:Qの大会管理データv2
```

フォームID、編集URL、個人状態を行1の右側へ追加しない。フォーム回答列が増減しても、
管理ブロックは回答終端の下で開始・終了マーカーと固定ラベルから特定する。

## 管理ブロックの列

| 行種別 | A:Q |
|---|---|
| `大会` | 種別、項目名、値 |
| A〜E級 | 級、参加費、開催日、schedule ID、申込期限、会内振込期限、本振込期限、支払時期、抽選結果日、会場、受付締切、公認、振込先、同期状態、最終同期日時 |
| `申込` | 種別、回答行、実メール、氏名、級、シート状態、player ID、entry ID、schedule ID、キャンセル日時、請求額、支払済額、残額、支払状態、同期状態、最終同期日時、同期エラー |
| `案内` | 種別、announcement ID、件名、フォームURL、Gmail message ID、対象schedule IDs |
| `メール` | 種別、email job ID、種別、送信予定・処理開始・完了日時、announcement ID、対象schedule IDs、配信件数・成功件数・失敗件数 |
| `配信` | 種別、delivery ID、email job ID、entry ID、Gmail message/draft ID、下書き作成・処理開始・最終試行・送信日時、試行回数、最終エラー |
| `旧管理` | 種別、元セル、元見出し、型、値、数式、メモ、表示形式、背景色、入力規則 |

## データ対応表

| セクション | 保存項目 | 発生元・照合先 |
|---|---|---|
| 大会 | 大会名、tournament ID、申込処理完了、大会振込完了 | `tournaments` |
| 大会 | フォームID、公開URL、編集URL | Google Formと旧シート |
| 大会 | 公認、同期状態、最終同期日時、同期エラー | schedule、管理サイト |
| 日程 | 級、schedule ID、開催日、各期限、支払時期、参加費 | `tournament_schedules` |
| 日程 | 振込先、抽選結果日、会場、受付締切、公認 | `tournament_schedules` |
| 申込 | 回答行、メール、氏名、級、旧シート状態 | フォーム回答 |
| 申込 | player ID、entry ID、schedule ID、キャンセル日時 | `players`、`entries` |
| 申込 | 請求額、支払済額、残額、支払状態 | 支払い履歴からのAPI計算値 |
| 申込 | 同期状態、最終同期日時、同期エラー | 管理サイト同期 |
| 案内 | announcement ID、件名、フォームURL、Gmail ID、対象日程 | `announcements` |
| メール | job ID、種別、予定・処理・完了日時、対象日程、配信集計 | `email_jobs`、`email_deliveries` |
| 配信 | delivery ID、entry ID、Gmail message/draft ID、各処理日時、試行回数、エラー | `email_deliveries` |
| 旧管理監査 | 元セル、見出し、型、値、数式、メモ、書式、色、入力規則 | 廃止する旧管理列 |

## 移行条件

1. 大会、日程、申込・支払いサマリー、案内、メールジョブをAPIから全件取得する。
2. 大会名、級・開催日、疑似メールで旧シートとAPIを一意に照合する。
3. 候補0件・複数件、日程・参加費不一致、API部分取得では実行しない。
4. 非表示バックアップを作成する。
5. 新管理ブロックを書き、再読取して件数と主要IDを照合する。
6. 検証成功後だけ旧管理列と旧下部固定領域を撤去する。
7. 失敗時は同じシートをバックアップから復元する。

旧管理監査は復元・確認用であり、運用データの保存先ではない。大会・日程・申込・支払・
案内・メールの値は、それぞれの正規セクションへ記録する。

## 同期

- 新規フォーム回答は回答原本へ先に保存し、v2申込行を追加する。
- API停止時はIDを推測せず`pending_api`とエラーを記録する。
- 年度完全同期はv2の日程・期限・参加費・申込状態をAPIへ送り、成功後に支払い履歴集計、
  案内、メール、配信結果を同じv2ブロックへ書き戻す。
- APIだけ、またはシートだけ更新できた場合は`partial`として完了扱いしない。
- 支払い正規履歴はAPIの`entry_payments`に置き、シートには請求・支払済・残額・計算状態を
  復旧可能な運用記録として保持する。

DB/APIから取得できない状態を空欄で成功扱いしてはならない。未同期の場合は明示的な
同期状態として記録し、IDや金額を推測しない。
