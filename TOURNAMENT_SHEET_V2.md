# 大会回答シート管理構造 v2

フォーム回答と「振込み済みか」列はその場に残し、旧右側管理列と旧下部領域にあった
大会設定だけを回答終端後のA:Cへ置く。DBの内容やフォーム回答は複製しない。

## 正本

- フォーム回答：上部のGoogleフォーム回答表
- 選考の手入力状態：回答表直後の既存「振込み済みか」列
- 支払状態・入金履歴：DB/API
- 大会・日程の設定：回答終端後のA:C管理ブロック
- tournament、schedule、player、entry、支払、案内、メール、配信：DB/API
- フォーム本体：Googleフォーム

## 物理構造

```text
行1       フォーム回答ヘッダー | 振込み済みか | 空白3列
行2〜R    フォーム回答         | 手入力状態   | 空白3列
行R+1     空行
行R+2〜   A:Cの大会設定
```

管理ブロックは`__TAIKAI_MANAGEMENT_V2__`から
`__TAIKAI_MANAGEMENT_V2_END__`までとし、全行を「種別・項目・値」の3列にする。

大会設定はフォームID、フォーム編集URL、公認、申込開始日、リマインダー、本申込期限、
抽選日、本振込期限、大会の日時、メモ、後納制、振込先を持つ。級別設定は
`日程:A`〜`日程:E`ごとに級、参加費、開催日だけを持つ。

回答者のメール・氏名・級、player/entry/schedule ID、支払集計、案内・メール・配信履歴、
同期状態は管理ブロックへ書かない。必要時にDB/APIから取得する。

## 実装責務

- 大会詳細は回答・選考状態・設定をシートから、支払・申込IDをDBから読む。
- 完全同期は回答とA:C設定からDB入力を作り、DB内容をシートへ複製しない。
- 出納更新はDBだけを更新し、申込行をシート下部へ書き戻さない。
- 公認・参加費・開催日の編集はA:C設定を更新した後、該当DB日程へ同期する。
- 削除時のtournament/schedule IDはDBから取得する。
- 新規フォーム作成もこの構造だけを生成する。
- 旧シートの移行は`taikai_manage`の`migrate_tournament_sheet(sheet_name)`で行う。
