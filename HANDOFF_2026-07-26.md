# 2026-07-26 作業引き継ぎ

## 現在地

管理サイトを `taikai_manage` の新DB APIへ移行する作業を進めた。DB同期、参加者参照、
参加費、支払い履歴、疑似メール移行のローカル実装とテストまでは完了している。
本番反映と実データ移行は未実施。

秘密鍵・実メール・DBパスワードなどの実値は、この文書、Git、Issueへ記録しない。

## 管理サイト（keiokaruta_manage_site）

今回の主要コミット:

- `d5ed4e6` Pseudonymize player emails at API boundary
- `76cfa8e` Add player email migration workflow
- `cc28c41` Sync schedule participation fees from sheets
- `02ce0fa` Prefer latest registration across site workflows
- `0ca9cbc` Add player tournament participation matrix

実装済み:

- 実メールをHMAC-SHA256で疑似化してからAPIへ送信
- 疑似メール形式は `v1-<60桁hex>@example.invalid`
  - HMAC-SHA256の先頭240 bit
  - `v1-`込み63文字とし、メールのローカル部64文字制限内に収める
- `PSEUDO_EMAIL_SECRET`はGAS Script Propertiesから取得
- 秘密鍵未設定時は実メールへフォールバックせず停止
- `DBメール対応表`を初回利用時に自動作成し、非表示化
- フォーム回答、個別登録、年度同期で疑似メールを使用
- DB名簿表示ではローカル対応表から実メールを逆引き
- APIデバッグレスポンスの実メールを`[redacted]`へ置換
- DB管理ページに疑似メール移行のdry-run・本実行UIを追加
- 年度同期では今年度の全大会を開催日基準で同期
- schedule単位の参加費を大会シートから取得して同期

作業ツリー:

- `.claspignore`だけがユーザー所有の未コミット変更として残っている
- 今回のコミットには含めていない

## taikai_manage

Issue #42としてローカル実装済み:

- `POST /admin/player-email-migration`
- dry-run、存在検査、疑似メール形式検査、重複・衝突検査
- 本実行はトランザクション
- 同じ入力の再実行は更新0件で成功
- player IDを変えないためentries、payments、deposits等の関連を維持
- 既存メールワーカーは疑似メールを配送先に使おうとすると停止
- 統合テスト `database/api/tests/player-email-migration.sh`

重要: `taikai_manage`には参加費・支払い・デポジット等の大規模な未コミット変更が
先に存在する。Issue #42はその変更と同じファイルにも重なっているため、今回こちらから
一括コミットしていない。現在の作業ツリーを破棄・resetしないこと。

ローカル検証結果:

- `npm test`: 全テスト通過
- `bash database/api/tests/run-all.sh`: 全テスト通過
- 疑似メール移行のdry-run、本実行、再実行、形式エラーをDocker DBで確認

## Issue整理

### 優先順

1. `taikai_manage#42` 疑似メール移行
   - ローカル実装済み、本番配置・実移行待ち
2. `taikai_manage#40` schedule参加費
   - ローカル実装・テスト済み、コミット・DB migration・本番配置待ち
3. `taikai_manage#41` 支払い・デポジット履歴
   - ローカル実装・テスト済み、コミット・DB migration・本番配置待ち
4. `taikai_manage#36` メールジョブ
   - 疑似メールをGAS側で実メールへ逆引きして配送するワーカーが未完了
   - `ScriptApp.getProjectTriggers()`等をコードから呼ばない
5. `taikai_manage#39` 最新登録ルールのAPI書込統一
   - 未完了
6. `keiokaruta_manage_site#3` 参加費説明ページ
   - #40・#41の本番仕様確定後

`keiokaruta_manage_site#2`はサイト側実装と移行UIまで完了しているが、
本番移行とメール送信逆引きが終わるまで閉じない。

## 次回の安全な本番反映順序

1. `taikai_manage`の全未コミット差分をレビューする
2. 参加費・支払い・疑似メール移行を意味のある単位でコミットする
3. 本番DBをバックアップする
4. migration SQLを番号順に本番DBへ適用する
5. AwardSpaceへAPIコードを配置する
6. `/health`と既存の読取APIを確認する
7. 管理サイトで`clasp push`する
8. バージョン固定Webアプリなら新バージョンを再デプロイする
9. DB管理ページの「移行内容を検査」を実行する
10. 対象件数と移行済み件数を確認する
11. 「検査した内容で移行」を実行する
12. DB上のplayers.emailがすべて疑似メールになったことを確認する
13. 名簿、選手別出場大会、年度同期プレビューを確認する
14. 問題がなければ年度同期を実行する

移行前に年度同期や新規フォーム登録を実行すると、旧実メールの選手と疑似メールの
選手が別人として併存する可能性がある。API配置後、疑似メール移行を先に完了する。

## メールに関する未完了点

現在は疑似メールへの誤配送を防ぐfail-closedまで実装済み。振込確認等を実際に送るには、
管理サイト側のGASワーカーがdispatch contextのplayer IDまたは疑似メールを受け取り、
`DBメール対応表`から実メールを一意に逆引きしてGmail送信する必要がある。

逆引き不能、複数一致、秘密鍵未設定の場合は送信せず、配信失敗として記録する。
既存の `ensure_email_worker_trigger()` は `ScriptApp.getProjectTriggers()` を呼ぶため、
管理サイトへ移植しない。トリガーが必要ならApps Script画面から手動設定する。

## 機密情報

- `PSEUDO_EMAIL_SECRET`は管理ページ側GASのScript Propertiesに設定済みとのこと
- その実値は確認・出力・再保存しない
- `clasp push`はScript Propertiesを変更しない
- 秘密鍵を変更すると疑似メールが変わり、別選手扱いになるためローテーションは移行設計が必要
