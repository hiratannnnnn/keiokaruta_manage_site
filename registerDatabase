// 旧メニュー互換エントリポイント。
// 実際の登録処理はserver/RegisterDatabase.jsの新API実装へ集約する。
function registerDatabase(state = false, kounin, name = '') {
  const sheetName = name || SpreadsheetApp.getActiveSheet().getName();
  return runRegisterDatabase(sheetName, kounin);
}
